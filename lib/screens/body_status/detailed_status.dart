import 'package:flutter/material.dart';

class DetailedStatus extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('detailed Status'),
    );
  }
}
